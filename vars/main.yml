---
# Common
postgresql_version_major: "{{ postgresql_version.split('.')[0] }}"

# RedHat
postgresql_conf_dir_default: /var/lib/pgsql/{{ postgresql_version_major }}/data
postgresql_pgdata_default: /var/lib/pgsql/{{ postgresql_version_major }}/data

# All
